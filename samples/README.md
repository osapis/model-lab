# 演示样例

这两个样例由 AI 生成，用于展示 HTML/SVG 预览和推理答案。它们没有调用用户配置的 API，不计入 API 实测统计；没有编造耗时、Token 数或评分。

- `pelican.html`：按照鹈鹕骑自行车的提示词生成的 HTML/SVG 动画。
- `reasoning.txt`：按照黑袋糖果题生成的文字回答，保留原始答案。

Node / Docker 首次启动会加载这些演示记录。Cloudflare 首次安装仅创建两道提示词，不创建演示测试记录。管理员可以删除演示记录，或者在结果广场选择「API 实测」筛选真实调用。
