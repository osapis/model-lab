# 安全说明

## 报告漏洞

请优先使用本仓库 **Security → Report a vulnerability** 私密报告漏洞。若该入口尚未开放，可以创建一个仅请求私密联系渠道的 Issue；不要在公开 Issue 中贴出漏洞利用细节、API Key、管理口令、会话 Cookie、数据库或未脱敏的配置备份。

报告中请说明版本／提交、部署方式（Docker 或 Cloudflare）、受影响的接口和可使用虚构数据重现的步骤。维护范围为当前主分支及最新发布版本；提交修复时请附可重复的验证步骤。

## 数据边界

- 结果广场用于公开展示测试题目与模型输出。不要把密码、个人信息或其他不宜公开的数据放进提示词；模型输出也可能包含请求中的内容。
- API 密钥和存储密钥由后端保存，公共 API 使用明确的字段投影。管理员页面的部分 Key 预览仅供登录后的管理员核对。
- HTML／SVG 输出是不可信内容，预览应始终保持沙箱隔离。修改预览功能时不要放开同源权限或把生成内容直接插入管理页面。
- 加密配置导出包含恢复部署需要的敏感配置。导出文件与解密密码应分开保管；不要将两者提交到仓库。

## 部署与维护

使用独立、足够长的管理口令，并通过 HTTPS 访问后台。Docker 的数据目录和 Cloudflare 的管理令牌、Worker Secrets 均应限制访问。AES-GCM 加密不意味着同时拿到数据库和加密主密钥的人无法读取配置。

Cloudflare API Token 应按部署需要授予权限，通过本机权限受限的文件或环境变量提供；不要写入 Wrangler 配置、命令参数、截图或 AI 的公开输出。仅在用户授权的账户和资源中执行部署。

本仓库不包含维护者的运行数据库、真实服务配置、密钥或历史测评记录。`.gitignore` 和 `.dockerignore` 不能清除已经提交过的秘密：若误提交，请立即在对应服务中撤销／轮换凭证，再清理 Git 历史及发布产物。

部署到 Cloudflare 或通过测试不构成绝对安全保证。更新依赖、限制管理员访问、检查实际公开接口并妥善备份，是部署者需要持续维护的事项。
